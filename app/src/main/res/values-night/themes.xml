<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark theme with glassmorphism support -->
    <style name="Theme.GitCommitBuddy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand colour — GitHub green (Dark variant) -->
        <item name="colorPrimary">@color/md_theme_dark_primary</item>
        <item name="colorOnPrimary">@color/md_theme_dark_onPrimary</item>
        <item name="colorPrimaryContainer">@color/md_theme_dark_primary</item>
        <item name="colorOnPrimaryContainer">@color/md_theme_dark_onPrimary</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/md_theme_dark_secondary</item>
        <item name="colorOnSecondary">@color/md_theme_dark_onSecondary</item>

        <!-- Background & Surface -->
        <item name="android:colorBackground">@color/md_theme_dark_background</item>
        <item name="colorSurface">@color/md_theme_dark_surface</item>
        <item name="colorOnSurface">@color/md_theme_dark_onSurface</item>

        <!-- Error -->
        <item name="colorError">@color/md_theme_dark_error</item>

        <!-- Status bar & Nav bar - Fully transparent for gradient bleed -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

        <!-- Window -->
        <item name="android:windowAnimationStyle">@style/WindowAnimationStyle</item>
    </style>
</resources>
